{
  "name": "parasapi",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ tesol2y090/parasapi",
    "create-local": "graph create --node http://localhost:8020/ tesol2y090/parasapi",
    "remove-local": "graph remove --node http://localhost:8020/ tesol2y090/parasapi",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tesol2y090/parasapi",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  }
}
